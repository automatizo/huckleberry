require 'spec_helper'

describe Huckleberry do
  it 'has a version number' do
    expect(Huckleberry::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
